#+Title: Tyler Burns Website
#+Author: Tyler Burns
#+Date: May 22, 2026
#+Purpose: tasks and lab notebook

* Tasks
** DONE refactor the mobile version of game mode
CLOSED: [2026-06-01 Mon 07:44] SCHEDULED: <2026-05-31 Sun>
Esp the social media mode.
** DONE consider little mini-game challenges
CLOSED: [2026-06-01 Mon 07:44]
Platformers, "insert" and so forth.
** TODO find the little path bugs and patch them
** TODO add the talisman system to the regular game mode
** TODO add little virus gremlin in social media mode that takes you to viral posts
SCHEDULED: <2026-05-31 Sun>
** TODO make the nose pick game mobile friendly
SCHEDULED: <2026-05-31 Sun>
** TODO add memory persistence with game mode toggling (which right now we only have with the back button)
SCHEDULED: <2026-06-01 Mon>
** DONE finish integrating Zelda mode in with game mode
CLOSED: [2026-06-01 Mon 07:44] SCHEDULED: <2026-06-01 Mon>
Ran out of tokens.
** DONE fix the teleporation bug in Zelda mode (integrate the two a bit better)
CLOSED: [2026-06-01 Mon 07:44] SCHEDULED: <2026-06-01 Mon>
Ran out of tokens.
** TODO integrate the old man sprite in
Just ask the agents what they want with it.
** TODO make a LI post of Social Mode
SCHEDULED: <2026-06-01 Mon>
* Log
** 2026
*** May
**** May 23
Added game mode to the site as of today. Hidden link in the about section at a period.
**** May 24
***** LI post for game mode
Who said that scrolling was best? Here I'm building out a "game mode" for my website, that gives you a Mario World style map you can use to navigate my content.

This map is divided into specific biomes that track to categories of work I share on my site (bioinformatics, philosophy, etc). Watch out for the gremlins! If one gets you, you'll get transported to a random piece of content.

The point is, there are way more ways to explore the internet than merely browsing through bottomless walls of media. What if social media had a mode like this?
***** LI post, claude edits
Who said that scrolling was best? I built a "game mode" for my website. A Mario World style overworld map you can use to navigate my content. Tap anywhere to move the cat. Each biome is a category of my work (bioinformatics, philosophy, etc).

Watch out for the green gremlins wandering the map. If one catches you, you get teleported to a random piece of content. Hopefully a good one.

Link is in the comments below.

The point: there are more ways to explore the web than bottomless feeds. The web is supposed to be a network of places, not a single river of media. What if every website gave you a map like this?
***** general idea: AR mode
One general idea that would be cool is to geocache each article and have to go to a place to get access to it or a collections of articles. I think you would need a legit mobile app but can a website get your GPS location? That sounds sketch. Let's check real quick.

Check reveals that this is indeed possible with a static html page. Good.
***** where to put game mode
Option 1, as a hidden link.
Option 2, as a direct link on the website.

We should eventually hit option 2.
***** secret room
We need a place to put my secret Marcus Aurelius Chapter 1 post. We'll have to sit with this one for a minute.
***** more elaborate game mode
We made the game map much bigger and added a talisman system that unlocks personal essays (because personal is valuable). We can expand this but the concept is there and fine as is.
***** back to the smaller map
We are thinking that the better user experience is the smaller map, though we are open to ideas. We can add the talisman system to the smaller map and it might be a bit more manageable. It depends on whether the user wants a handle on my stuff or wants to get lost in a map. Either way, I think we are done for the day. This was a good session, if only to get a feel for the different possibilities here.
**** May 29
***** Social Media World kickoff
Started planning a second overworld for the LinkedIn posts collection, reachable only from game mode. Captured the work under the "Social Media World" heading above. Big open question is architecture: a separate HTML file forked from game_mode.html vs. refactoring the engine to be data-driven over a "world" object.
***** game-mode toggle polish (done earlier today)
Centered the "Regular Mode" button so it no longer collides with the "Tyler's World" title, renamed "Start Beach" -> "Start Crossing" (it's shaped like a cross), tightened the spacing between the Light/Dark and Game Mode buttons, and made the game/regular toggle remember your choice (home page redirects to game mode if that was your last pick).
**** May 30
***** Social Media World shipped (first playable)
Built the second overworld end to end against the shared-engine plan.

- Extracted the engine from game_mode.html into js/engine.js as
  createWorld(config). Structural CSS moved to css/game.css. game_mode.html
  dropped from ~2900 to ~1250 lines and now just supplies its data as a config.
  Verified it boots identically (48x32, 126 levels, 4 gremlins, no JS errors).
- The Collections "Social media posts" tile is now the portal into the new
  world; a Gateway tile in the social world is the portal back out.
- Generated the social world from social_media_posts.org: 10 zones (H1s) + a
  Gateway, 85 levels (H2s) with tokens/dates/summaries/LinkedIn urls, on a
  24x47 grid with a serpentine layout and a trunk road. Flood-fill confirms
  every tile is reachable.
- Per-zone palettes, a cyan gremlin, and themed HUD copy. No speedster in the
  social world (the speedster is now opt-in per world via config).
- Files all live in public/ alongside game_mode.html (engine.js, game.css,
  social_world.html), so the org-publish build leaves them untouched.

Left for later: a real-browser/Safari/mobile pass on device. The "nose pick game
mobile friendly" task is unrelated and was not touched.
***** Social Media World — layout + portal + warp revision
Reworked per feedback:
- Confirmed the 10 zones map 1:1 to the literal H1 categories, and switched the
  zone names from invented ones to faithful shortenings of the real categories
  (e.g. "Dimensionality Reduction", "Clustering & Gating", "Workflow & QC").
- Replaced the vertical stack with a grid-of-zones layout comparable to the main
  world: a 3x4 macro grid (Gateway + 10 zones) on a 35x40 map, zones joined by
  bridges across water gaps.
- Made the in-zone passageways more connective: posts in each zone are laid in a
  mini-grid and lattice-paved to their right + down neighbours, so each zone is a
  lattice of paths rather than a single line.
- Portal box is now the SAME shape as every other level tile, just a distinct
  magenta colour (dropped the spinning-gradient look). The main-world tile token
  is "SP".
- Bridge plank orientation is now derived from walkable neighbours instead of a
  main-world-specific hardcode, so bridges look right in both worlds.
All verified headlessly: both worlds render with no JS errors, every tile is
reachable, and the warp animation fires in both directions.
***** Social Media World — hidden-path entry (Zelda-style)
Reworked the entry to mirror the Marcus Aurelius secret:
- The "SP" box is now an ordinary tile linking to the text collection
  (social_media_posts.html), not the portal itself.
- The teleport is a HIDDEN portal one tile further right: standing on the SP box
  and pressing right reveals a path tile (41,13) and the portal tile (42,13),
  which warps to the Social Media World. New SECRETS entry "socialworld".
- The social world's exit portal now returns to 42,13 (the hidden teleport
  square); sessionStorage keeps the secret open within the session so the return
  square is walkable. Also thinned the social-world paths (horizontal tracks +
  scattered vertical spines, not a full grid) and gave the portal box a black
  outline like the other tiles.
**** May 31
***** Zelda-style secrets experiment — rolled back
/(Written by Claude.)/ Prototyped a generalized secret system (auto-derived
path tiles, a kind:"treasure" easter-egg tile, the Zelda essay as a hidden room,
and a treasure in the Fun Arcade). Tyler didn't like it, so we rolled it all
back: engine.js, game.css, and game_mode.html are restored to their prior state.
(Note: public/zelda_mode.html was made by a separate Claude Code instance and is
kept — it was not part of this change.)
